---
applyTo: '**'
---
Always follow the instructions given by the user. or try to suggest better alternatives if you think the user is making a mistake.
make use of mcps always mostly playwright for testing.
and make use of ui mcps for ui related tasks.
Always find the root cause and fix the root cause rather than patching the issue. prefer fixing root cause over patching.
Always try implementing in chunks to avoid file corruption
Always Use Windows PowerShell commands in this project. and dont use the && for commands continuity
always try to combine commands in one line using `;` to avoid switching terminals. 
when running servers, open a new terminal for each server instead of stopping one to start another.
also if you close a terminal, you will have to restart the server in a new terminal.
if a server is running in a terminal Always use another terminal to run other commands.
try to fix errors and lints when posisble before asking for help.
dont just delete files or code blocks to fix errors.
whenever working on the code dont mess with code that is not related to your task.
also never messup code that is working fine. dont mess up codebase
ALWAYS UNDERSTAND AND INDEX CODEBASE FOR BETTER UNDERSTANDING.
try to fix the error if the change causes error revert back the change and try again. try to avoid deleteing and creationg new files only Do THIS WHEN NEEDED.
dont use if else or do commands in powershell as it does not support it.
to test build always use this logic navigate to the appropriate directory ; npmk run build or etc whatever your command is.
dont ask again and again for the same task once confirmed by the user complete the task completely and stop only after the task is complete
complete the task in todos so bigs chunks are divided into smaller chunks. but these todos should complete large amount of task so the user intervention is required less and only when it is neccessary.